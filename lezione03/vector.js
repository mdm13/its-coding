function sumArray(v) {
    
    var sum=0
    for(var i=0;i<v.length;i++) {
        sum+=v[i]
    }

    return sum
}

function concatWords(v) {
    
    var sum=''
    for(var i=0;i<v.length;i++) {
        sum+=v[i]
    }

    return sum
}
